import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    fontSize: 30,
    fontWeight: 'bold',
    textAlign: 'center',
    color: '#E5BF3C',
    marginBottom: 20,
  },
  image: {
    resizeMode: 'contain',
    margin: 20,
    height: 180,
  }

});